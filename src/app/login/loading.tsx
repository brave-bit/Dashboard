import { LoadingSpinner } from "@/components/ui/LoadingSpinner";

export default function LoginLoading() {
  return (
    <LoadingSpinner className="flex flex-1 items-center justify-center py-12" />
  );
}
